Examples for makefile intro session

Simple rule 

target: dependency
        action

The dependency can either be a file or another target. If it does not exist the make will fail. In this example, the dependency is another file.
